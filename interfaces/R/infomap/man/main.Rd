% Generated by hand — keep in sync with R/helpers.R.
\name{main}
\alias{main}
\title{Run Infomap from the command line}
\usage{
main(args = NULL)
}
\arguments{
\item{args}{Character vector of CLI arguments. Defaults to
\code{commandArgs(trailingOnly = TRUE)}.}
}
\value{
Invisibly returns the resulting \code{Infomap} instance.
}
\description{
Parses arguments from \code{commandArgs()} (or the \code{args} parameter), runs
Infomap, and returns the result. This is mainly used by the bundled
\code{inst/exec/infomap} shim and is exported so users can call it from
Rscript:
}
\details{
\preformatted{Rscript -e 'infomap::main()' --args -- <flags>
}
}
