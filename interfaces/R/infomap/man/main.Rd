% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{main}
\alias{main}
\title{Run Infomap from the command line}
\usage{
main(args = NULL)
}
\arguments{
\item{args}{Character vector of CLI arguments (default: from \code{commandArgs()}).}
}
\value{
Invisibly returns the resulting \code{Infomap} instance.
}
\description{
Parses arguments from \code{commandArgs()} (or the \code{args} parameter), runs
Infomap, and exits. Useful for invoking the optimizer from \code{Rscript}:
\verb{Rscript -e 'infomap::main()' --args <flags...>}.
}
\details{
Each element of \code{args} is treated as a single token; spaces inside a
token (e.g. in filenames) survive via \code{shQuote}. Pass tokens, not a
single pre-joined string.
}
