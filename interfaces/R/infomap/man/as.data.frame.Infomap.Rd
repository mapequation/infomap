% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_data_frame.R
\name{as.data.frame.Infomap}
\alias{as.data.frame.Infomap}
\title{Coerce Infomap results to a data.frame}
\usage{
\method{as.data.frame}{Infomap}(
  x,
  row.names = NULL,
  optional = FALSE,
  states = TRUE,
  depth_level = 1L,
  tibble = FALSE,
  ...
)
}
\arguments{
\item{x}{An \code{Infomap} instance after \code{run()} has been called.}

\item{row.names}{Standard \code{as.data.frame} argument; ignored.}

\item{optional}{Standard \code{as.data.frame} argument; ignored.}

\item{states}{If \code{TRUE}, return one row per state node (for
higher-order networks); otherwise merge state nodes with the same
physical id within a module. Default \code{TRUE}.}

\item{depth_level}{Tree depth used for the \code{module_id} column. \code{1}
gives top-level modules, \code{-1} the bottom level. Default \code{1}.}

\item{tibble}{If \code{TRUE}, return a \code{tibble} (requires the \code{tibble}
package). Default \code{FALSE} returns a plain \code{data.frame} so the
return type is independent of installed packages.}

\item{...}{Unused.}
}
\value{
A \code{data.frame} (or a \code{tibble} when \code{tibble = TRUE}) with
columns \code{state_id}, \code{node_id}, \code{module_id}, \code{flow}, \code{name} and
(for multilayer networks) \code{layer_id}.
}
\description{
Returns one row per leaf node in the partitioned tree. Mirrors
Python's \code{Infomap.get_dataframe()}.
}
\examples{
im <- Infomap(silent = TRUE)
im$add_links(list(c(1, 2), c(2, 3), c(3, 1), c(3, 4), c(4, 5), c(5, 6), c(6, 4)))
im$run()
as.data.frame(im)
}
