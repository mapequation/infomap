% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_infomap.R
\name{cluster_infomap_multilayer}
\alias{cluster_infomap_multilayer}
\title{Cluster a multilayer network with Infomap}
\usage{
cluster_infomap_multilayer(
  multilayer_edges,
  weight = NULL,
  args = NULL,
  opts = NULL,
  tibble = FALSE,
  ...
)
}
\arguments{
\item{multilayer_edges}{A data frame, tibble, or matrix of multilayer
edges in one of the formats above.}

\item{weight}{Edge weight column name or numeric column index. Set
\code{FALSE} to ignore weights. If \code{NULL} (default), uses a data-frame column
named \code{weight} when present, then falls back to a single non-format
data-frame column, then to weight \code{1}.}

\item{args}{Optional raw CLI argument string passed to \code{\link[=Infomap]{Infomap()}}.}

\item{opts}{Optional options list from \code{\link[=infomap_options]{infomap_options()}}.}

\item{tibble}{If \code{TRUE}, return \code{nodes} as a tibble (requires the
\code{tibble} package). Default \code{FALSE} returns a plain data frame.}

\item{...}{Named option overrides forwarded to \code{\link[=Infomap]{Infomap()}}.}
}
\value{
An object of class \code{"infomap_result"}. The \code{nodes} field
includes a \code{layer_id} column.
}
\description{
High-level helper for multilayer networks: pass a single data frame
(or matrix / tibble) of multilayer edges, run Infomap, and get an
\code{infomap_result} whose \code{nodes} data frame includes a \code{layer_id}
column.
}
\details{
Two input formats are recognised by column name (case-sensitive):
\itemize{
\item \strong{Full multilayer}: columns \code{layer_from}, \code{node_from}, \code{layer_to},
\code{node_to}, plus an optional weight column. Each row is an arbitrary
\verb{(layer, node) -> (layer, node)} link, routed via
\code{Infomap$add_multilayer_links()}. Use this format to mix
intra-layer links (same \code{layer_from}/\code{layer_to}) and inter-layer
links (same \code{node_from}/\code{node_to} across different layers) in a
single edge list.
\item \strong{Intra-layer only}: columns \code{layer}, \code{node_from}, \code{node_to}, plus
an optional weight column. Every row is a link within one layer,
routed via \code{Infomap$add_multilayer_intra_links()}. With no explicit
inter-layer links, Infomap couples layers via
\code{multilayer_relax_rate} (default 0.15).
}

Data-frame columns may appear in any order. Matrix inputs are interpreted
positionally with 3 columns for unweighted intra-layer links and 4 or 5
columns for full multilayer links. Use a data frame for weighted
intra-layer-only inputs.

For more exotic setups (e.g. weighted inter-layer couplings via
\code{add_multilayer_inter_links()}), use the lower-level \code{\link[=Infomap]{Infomap()}} R6
API directly.
}
\examples{
# Intra-layer only: two triangles, one per layer.
intra <- data.frame(
  layer     = c(1, 1, 1, 2, 2, 2),
  node_from = c(1, 2, 3, 1, 2, 3),
  node_to   = c(2, 3, 1, 2, 3, 1)
)
result <- cluster_infomap_multilayer(intra, silent = TRUE, num_trials = 3)
result$codelength
as.data.frame(result)

# Full multilayer: intra-layer triangles + inter-layer couplings.
edges <- data.frame(
  layer_from = c(1, 1, 1, 2, 2, 2, 1, 1, 1),
  node_from  = c(1, 2, 3, 1, 2, 3, 1, 2, 3),
  layer_to   = c(1, 1, 1, 2, 2, 2, 2, 2, 2),
  node_to    = c(2, 3, 1, 2, 3, 1, 1, 2, 3),
  weight     = c(1, 1, 1, 1, 1, 1, 0.5, 0.5, 0.5)
)
result <- cluster_infomap_multilayer(edges, silent = TRUE, num_trials = 3)
result$num_top_modules
}
