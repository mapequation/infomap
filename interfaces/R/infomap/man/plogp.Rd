% Generated by hand — keep in sync with R/helpers.R.
\name{plogp}
\alias{plogp}
\alias{entropy}
\alias{perplexity}
\title{Information-theoretic helpers}
\usage{
plogp(p)

entropy(p)

perplexity(p)
}
\arguments{
\item{p}{Numeric vector of probabilities.}
}
\value{
\code{plogp}: numeric vector of \eqn{p \log_2 p} values (0 for non-positive
entries).

\code{entropy}: numeric scalar, Shannon entropy in bits.

\code{perplexity}: numeric scalar, \eqn{2^{H(p)}}.
}
\description{
\code{plogp()} computes \eqn{p \log_2 p} element-wise, returning 0 for
non-positive entries. \code{entropy()} computes Shannon entropy in bits.
\code{perplexity()} returns \eqn{2^{H(p)}}.
}
\examples{
plogp(c(0.5, 0.5))
entropy(c(0.5, 0.5))
perplexity(c(0.5, 0.5))
}
