% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_infomap.R
\name{cluster_infomap}
\alias{cluster_infomap}
\alias{print.infomap_result}
\alias{summary.infomap_result}
\alias{as.data.frame.infomap_result}
\title{Cluster a network with Infomap}
\usage{
cluster_infomap(
  edges,
  weight = NULL,
  e.weights = NULL,
  v.weights = NULL,
  nb.trials = NULL,
  args = NULL,
  opts = NULL,
  tibble = FALSE,
  ...
)

\method{print}{infomap_result}(x, ...)

\method{summary}{infomap_result}(object, ...)

\method{as.data.frame}{infomap_result}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{edges}{A data frame, matrix, or igraph graph.}

\item{weight}{Edge weight column. For data frames, use a column name or
numeric column index. For matrices, use a numeric column index. Set
\code{FALSE} to ignore weights. For igraph graphs, use the edge attribute
name, or \code{NULL} to use \code{"weight"} when present.}

\item{e.weights}{Alias for \code{weight}, provided for familiarity with
\code{igraph::cluster_infomap()}. For igraph inputs, this can be an edge
attribute name or a numeric vector with one value per edge. Do not
pass both \code{weight} and \code{e.weights}.}

\item{v.weights}{Vertex weights are not supported by this high-level
helper yet.}

\item{nb.trials}{Alias for the Infomap \code{num_trials} option, provided for
familiarity with \code{igraph::cluster_infomap()}. Do not pass both
\code{nb.trials} and \code{num_trials}.}

\item{args}{Optional raw CLI argument string passed to \code{\link[=Infomap]{Infomap()}}.}

\item{opts}{Optional options list from \code{\link[=infomap_options]{infomap_options()}}.}

\item{tibble}{If \code{TRUE}, return \code{nodes} as a tibble (requires the
\code{tibble} package). Default \code{FALSE} returns a plain data frame.}

\item{...}{Named option overrides forwarded to \code{\link[=Infomap]{Infomap()}}.}

\item{x}{An \code{infomap_result} object.}

\item{object}{An \code{infomap_result} object.}

\item{row.names}{Standard \code{as.data.frame} argument; ignored.}

\item{optional}{Standard \code{as.data.frame} argument; ignored.}
}
\value{
An object of class \code{"infomap_result"} with fields \code{nodes},
\code{modules}, \code{codelength}, \code{num_top_modules}, \code{model}, and related
summary fields.
}
\description{
High-level helper for the common case: pass an edge list, run Infomap,
and get a compact result object with node assignments and summary fields.
}
\details{
For \code{data.frame} and \code{matrix} inputs, the first two columns are treated
as source and target node ids. Node ids must be numeric or integer and
are passed through to the low-level \code{\link[=Infomap]{Infomap()}} API unchanged. If
\code{weight = NULL}, a third edge-list column is used as weight when present;
otherwise all links get weight \code{1}.

For \code{igraph} inputs, this function delegates to \code{Infomap$add_igraph()}.
Infomap results use R igraph's 1-indexed vertex ids; the \code{mapping} field
maps those ids back to vertex names when the graph has names.
}
\examples{
edges <- data.frame(
  source = c(1, 1, 2, 3, 4, 4, 5),
  target = c(2, 3, 3, 4, 5, 6, 6)
)
result <- cluster_infomap(edges, silent = TRUE, num_trials = 5)
result$codelength
result$modules
as.data.frame(result)
}
