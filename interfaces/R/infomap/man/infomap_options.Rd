% Generated by hand — keep in sync with R/options.R.
\name{infomap_options}
\alias{infomap_options}
\title{Build a reusable Infomap options list}
\usage{
infomap_options(...)
}
\arguments{
\item{...}{Named option overrides. Argument names mirror the Infomap CLI
flags with hyphens replaced by underscores (e.g. \code{num_trials},
\code{two_level}, \code{silent}). See the Infomap documentation for the full
list of available flags.}
}
\value{
A named list of Infomap options accepted by \code{\link{Infomap}()} and
\code{Infomap$run()}.
}
\description{
Returns a named list with one entry per Infomap CLI option. All
arguments default to the Infomap-internal defaults, so callers only
need to supply the options they want to override. The returned list
is accepted by \code{\link{Infomap}()} and \code{Infomap$run()}.
}
\details{
Argument names mirror the Infomap CLI flags (with hyphens replaced by
underscores) and the keyword arguments accepted by the Python interface.
}
\examples{
opts <- infomap_options(silent = TRUE, num_trials = 10, two_level = TRUE)
im <- Infomap(opts = opts)
im$add_links(list(c(1, 2), c(2, 3), c(3, 1)))
im$run()
}
\seealso{
\code{\link{construct_args}()}, \code{\link{Infomap}()}
}
