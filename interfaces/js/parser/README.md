# @mapequation/infomap-parser

Internal-supported parser helpers for `@mapequation/infomap` output formats.

This package is used by internal products and companion tooling. It is
smoke-built in CI, but it is not part of the public release automation surface
for this repository.
