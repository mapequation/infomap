# @mapequation/infomap-parser

Deprecated legacy parser helpers for `@mapequation/infomap` output formats.

This package is used by internal products and companion tooling. It is
smoke-built in CI, but it is private, not published from this repository, and
is no longer part of the public JavaScript surface.
